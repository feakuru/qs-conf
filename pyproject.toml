[project]
name = "quickshell"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "evdev>=1.9.2",
    "psutil>=7.1.3",
    "pyaudio>=0.2.14",
    "pyzmq>=27.1.0",
    "xkbcommon>=1.5.1",
]

[dependency-groups]
dev = [
    "mypy>=1.18.2",
    "pytest>=9.0.2",
]

[tool.mypy]
follow_untyped_imports = true
